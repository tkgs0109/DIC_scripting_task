<h1>最小単位タスク</h1>
<%= link_to 'TOP', tasks_path %>
<div class="taskDescendant">
  <% @tasks.each do |task| %>
    <p class="navigator">
      <% if task.level != 1 %>
        <% ids = task.clans %>
        <%= render 'navigator', ids: ids %>
      <% else %>
        <%= " > " %>
      <% end %>
    </p>
    <%= render 'taskpanel', task: task %>
  <% end %>
</div>
